##############################################################################################################################
# Ideas: 
##############################################################################################################################
 
def updateBits(n, m, i, j):
	allOnes = ~0
	left = allOnes << (j+1)
	right = (1<<i)-1
	mask = left | right
	n_cleared = n & mask
	m_shifted = m << i
	return n_cleared | m_shifted
	
##############################################################################################################################

def main():
	print '{0:b}'.format(updateBits(0b10000000000, 0b10011, 2, 6))

if __name__ == '__main__':
	main()